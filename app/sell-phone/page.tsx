"use client";
import { FormEvent,useState } from "react";
import { CheckCircle } from "@phosphor-icons/react";
import { brands } from "@/lib/products";

export default function SellPhonePage(){const[sent,setSent]=useState(false);const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const raw=Object.fromEntries(new FormData(event.currentTarget).entries());try{const response=await fetch("/api/used-phones",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...raw,pta_approved:raw.pta_approved==="true"})});const result=await response.json();if(!response.ok)throw new Error(result.error);setSent(true)}catch(reason){setError(reason instanceof Error?reason.message:"Could not submit request")}finally{setBusy(false)}}if(sent)return <main className="form-page shell"><div className="success-card"><CheckCircle weight="fill"/><h1>Request received.</h1><p>Our team will contact you after reviewing the phone details.</p></div></main>;return <main className="form-page shell"><span className="kicker dark">TRADE IN YOUR DEVICE</span><h1>Sell your phone</h1><div className="checkout-grid"><form className="checkout-form" onSubmit={submit}><div className="field-row"><label>Brand<select name="brand" required defaultValue=""><option value="" disabled>Select brand</option>{brands.map(b=><option key={b}>{b}</option>)}</select></label><label>Model<input name="model" required placeholder="e.g. Galaxy A56"/></label></div><div className="field-row"><label>Storage<select name="storage"><option>64 GB</option><option>128 GB</option><option>256 GB</option><option>512 GB</option></select></label><label>Condition<select name="condition"><option>Like new</option><option>Good</option><option>Fair</option><option>Needs repair</option></select></label></div><div className="field-row"><label>PTA status<select name="pta_approved"><option value="true">PTA approved</option><option value="false">Non-PTA</option></select></label><label>Expected price<input name="expected_price" type="number" min="0" placeholder="Rs."/></label></div><div className="field-row"><label>Your name<input name="full_name" required/></label><label>Phone number<input name="phone" required pattern="(?:\+?92|0)?3[0-9]{9}" placeholder="03XX XXXXXXX"/></label></div><label>City<input name="city"/></label><label>More details<textarea name="details" maxLength={1000} placeholder="Color, battery health, box/accessories and any faults"/></label>{error&&<p className="form-error">{error}</p>}<button disabled={busy} className="primary-btn wide">{busy?"Submitting…":"Submit valuation request"}</button></form><aside className="sell-aside"><h2>Turn your old phone into value.</h2><p>Tell us what you have. We review the details, contact you with an estimate, and guide you through a safe handover.</p><ol><li><b>1</b> Share phone details</li><li><b>2</b> Receive an estimate</li><li><b>3</b> Verify and get paid</li></ol></aside></div></main>}
